# Plan: RR-12

## Work Item
**Title:** Setup base Vue + Vite frontend for Rheanne Retriever

## Overview
RR-12 sets up the base frontend for Rheanne Retriever — a Vue 3 + Vite application that lets visitors ask questions about Rheanne's experience, powered by a Microsoft Foundry agent with knowledge of her CVs and projects. This branch covers planning, scaffolding, and cleaning up the boilerplate into a branded page shell ready for agent integration in a future branch.

## Phases

### Phase 1: Create branch plan
Document the implementation plan for RR-12 before any code is written.

- [X] Create `.claude/plans/RR-12.md` with phases covering scaffold, cleanup, and UI.

**Commit message:**
```
docs: add branch plan for RR-12

Document the implementation phases for the Rheanne Retriever base
frontend setup.

[RR-12]
```

### Phase 2: Scaffold Vue + Vite project
Bootstrap the project using the official Vite Vue template.

- [ ] Run `npm create vite@latest rheanne-retriever -- --template vue` to generate the base Vue 3 + Vite project.
- [ ] Run `npm install` to install dependencies.

**Commit message:**
```
build: scaffold Vue 3 + Vite project

Initialise the project using the official Vite Vue template as the
base for the Rheanne Retriever frontend.

[RR-12]
```

### Phase 3: Clean up boilerplate
Remove all default scaffold content so we start from a clean slate before building the real UI.

- [X] Delete `src/components/HelloWorld.vue`.
- [X] Remove default styles from `src/style.css`, keeping only a CSS reset and base font/colour variables.
- [X] Clear `src/App.vue` to a minimal shell (root layout only, no boilerplate content).
- [X] Remove unused assets (`vite.svg`, `vue.svg`) and update `index.html` title to "Rheanne Retriever".

**Commit message:**
```
refactor: remove Vite/Vue boilerplate

Strip out HelloWorld component, default styles, and unused assets to
prepare a clean base for the Rheanne Retriever UI.

[RR-12]
```

### Phase 4: Add branding and page shell
Build the core page structure — header with the app name and a short description so visitors immediately understand what the tool is for.

- [ ] Create `src/components/AppHeader.vue` with the "Rheanne Retriever" heading and a one-line description (e.g. "Ask me anything about Rheanne's experience").
- [ ] Add a `src/components/AppLayout.vue` (or equivalent wrapper) that composes header + main content slot.
- [ ] Wire `AppLayout` into `App.vue`.
- [ ] Add base styles: centred layout, readable font stack, brand colours.

**Commit message:**
```
feat: add branded header and page shell

Introduce AppHeader with the Rheanne Retriever heading and description,
and an AppLayout wrapper to provide a consistent centred page structure.

[RR-12]
```
